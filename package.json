{
  "devDependencies": {
    "tailwindcss": "^3.0.24"
  },
  "dependencies": {
    "tailwindcss-autofill": "^1.0.2",
    "tailwindcss-shadow-fill": "^1.0.1",
    "tailwindcss-text-fill": "^0.2.0"
  },
  "scripts": {
    "css:watch": "npx tailwindcss -i ./src/input.css -o ./src/styles.css --watch",
    "build": "npx tailwindcss -i ./src/input.css -o ./src/styles.css --minify",
    "predeploy": "git checkout deploy && git reset --hard develop && cp ./src/* ./ -r && rm input.css && rm -rf ./.vscode && rm -rf ./node_modules && rm -rf ./src && rm package.json && rm package-lock.json && rm tailwind.config.js && rm .gitignore",
    "deploy": "git add . && git commit -m 'auto-build' && git push --force",
    "postdeploy": "git checkout develop"
  }
}
